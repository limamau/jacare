[build-system]
requires = ["setuptools", "setuptools-scm"]
build-backend = "setuptools.build_meta"


[project]
name = "jacare"
version = "0.1.0"
dependencies = [
    "equinox",
    "h5py",
    "jax",
    "matplotlib",
    "numpy",
    "optax",
    "orbax",
    "pandas",
]
